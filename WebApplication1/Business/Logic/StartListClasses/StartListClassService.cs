using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using WebApplication1.Classes;
using WebApplication1.Models;

namespace WebApplication1.Business.Logic.StartListClasses
{
    public class StartListClassService
    {
        //public IOrderedEnumerable<StartListClass> GetAllContestsSorted(Models.Contest contest)
        //{
            //using (var db = new VaultingContext())
            //{
            //    return (from contestTable in db.StartListClasses
            //            select contestTable).Where(contest.ContestId).FirstOrDefault();

            //}
            //using (var db = new VaultingContext())
            //{

            //var country = contest?.Country;
            //var eventLocation = contest?.Location; 
            //var startListClasses = contest?.StartListClass ?? new List<StartListClass>();

            //    return startListClasses.OrderBy(x => x.StartOrder);
            //
        //}
    }
}